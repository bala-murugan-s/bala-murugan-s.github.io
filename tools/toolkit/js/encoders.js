export function encodeURL(str) { return encodeURIComponent(str); }
export function decodeURL(str) { return decodeURIComponent(str); }
export function encodeBase64(str) { return btoa(str); }
export function decodeBase64(str) { return atob(str); }